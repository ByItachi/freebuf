{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the splash and main windows",
  "windows": ["main", "splash", "error"],
  "permissions": ["core:default"]
}
